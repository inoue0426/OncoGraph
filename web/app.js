let d=[];const q=document.querySelector('#q'),r=document.querySelector('#results'),s=document.querySelector('#status');fetch('./data/search-index.json').then(x=>x.json()).then(x=>{d=x;s.textContent=`${d.length} indexed entities`});q.oninput=()=>{const t=q.value.toLowerCase();r.innerHTML=t?d.filter(x=>(x.name+' '+x.type+' '+(x.canonical_id||'')).toLowerCase().includes(t)).slice(0,50).map(x=>`<div class="card"><b>${x.name}</b><div class="meta">${x.type} · ${x.canonical_id||''}</div></div>`).join(''):''}